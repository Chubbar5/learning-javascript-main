function whoIsPaying(names){
    var people = [names];
    var position = 3 * Math.random;
    position= Math.floor;
    return position + "is paying for the meal."
};

function whoIsPaying(["Connor","Raffles", "Christiana"]);