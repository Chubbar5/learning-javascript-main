/* initial try
function argumentsLength(){
    var length = argumentsLength.length
    return length;
};

argumentsLength([5,2,1]);

*/
/**
 * @return {number}
 */
function argumentsLength() {
    return arguments.length
};

/**
 * argumentsLength(1, 2, 3); // 3
 */